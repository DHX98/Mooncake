diff --git a/mooncake-store/include/prefetch_throttle.h b/mooncake-store/include/prefetch_throttle.h
index 608be17..f76675c 100644
--- a/mooncake-store/include/prefetch_throttle.h
+++ b/mooncake-store/include/prefetch_throttle.h
@@ -200,7 +200,8 @@ class PrefetchThrottle {
         const Shard& shard = ShardFor(key);
         std::lock_guard<std::mutex> lock(shard.mutex);
         auto it = shard.entries.find(key);
-        if (it == shard.entries.end() || it->second.state != State::kCompleted) {
+        if (it == shard.entries.end() ||
+            it->second.state != State::kCompleted) {
             return -1;
         }
         return it->second.completed_ms;
@@ -274,8 +275,8 @@ class PrefetchThrottle {
             }
             return now - entry.trigger_ms > backoff_ms;
         }
-        const int64_t last_ms = entry.completed_ms >= 0 ? entry.completed_ms
-                                                        : entry.trigger_ms;
+        const int64_t last_ms =
+            entry.completed_ms >= 0 ? entry.completed_ms : entry.trigger_ms;
         return now - last_ms > ttl_ms;
     }
 
@@ -296,8 +297,7 @@ class PrefetchThrottle {
     }
 
     std::atomic<int64_t> cooldown_until_ms_{0};
-    std::atomic<int64_t> cooldown_ms_{DEFAULT_SSD_PREFETCH_COOLDOWN_SEC *
-                                     1000};
+    std::atomic<int64_t> cooldown_ms_{DEFAULT_SSD_PREFETCH_COOLDOWN_SEC * 1000};
     std::atomic<int64_t> dedup_ttl_ms_{DEFAULT_SSD_PREFETCH_DEDUP_TTL_SEC *
                                        1000};
     std::array<Shard, kNumShards> shards_;
diff --git a/mooncake-wheel/tests/test_prefetch_on_exist.py b/mooncake-wheel/tests/test_prefetch_on_exist.py
index bd305b9..038acf6 100644
--- a/mooncake-wheel/tests/test_prefetch_on_exist.py
+++ b/mooncake-wheel/tests/test_prefetch_on_exist.py
@@ -150,6 +150,7 @@ class TestPrefetchOnExist(unittest.TestCase):
 
     def _find_cold_key(self, candidates):
         """Poll until one of candidates is LOCAL_DISK-only."""
+
         def _probe():
             descs = self.store.batch_get_replica_desc(list(candidates))
             for key in candidates:
@@ -181,9 +182,7 @@ class TestPrefetchOnExist(unittest.TestCase):
         cold_key = self._find_cold_key(list(reference.keys()))
         expected = reference[cold_key]
 
-        self.assertEqual(
-            self.store.is_exist(cold_key, _prefetch_options(True)), 1
-        )
+        self.assertEqual(self.store.is_exist(cold_key, _prefetch_options(True)), 1)
 
         def _promoted():
             return self._has_memory_replica(cold_key), _replica_types(
@@ -197,9 +196,7 @@ class TestPrefetchOnExist(unittest.TestCase):
         )
 
         got = self.store.get(cold_key)
-        self.assertEqual(
-            bytes(got), expected, "get after prefetch must be bit-exact"
-        )
+        self.assertEqual(bytes(got), expected, "get after prefetch must be bit-exact")
 
     def test_exist_without_prefetch_does_not_promote(self):
         """Negative control: plain is_exist must not promote. Only
@@ -209,9 +206,7 @@ class TestPrefetchOnExist(unittest.TestCase):
         cold_key = self._find_cold_key(list(reference.keys()))
 
         self.assertEqual(self.store.is_exist(cold_key), 1)
-        self.assertEqual(
-            self.store.is_exist(cold_key, _prefetch_options(False)), 1
-        )
+        self.assertEqual(self.store.is_exist(cold_key, _prefetch_options(False)), 1)
 
         deadline = time.monotonic() + NEGATIVE_OBSERVE_SECONDS
         while time.monotonic() < deadline:
